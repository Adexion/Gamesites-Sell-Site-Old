<nav class="navbar navbar-expand-lg">
    <div class="container">
        <div class="navbar-logo">
            <img class="mw-100 mh-100" src="/assets/images/{{ logo }}" alt="Logotype">
        </div>
        {% if serverInfo | length != 0 %}
            <div class="d-flex justify-content-center align-items-center online">
                <span class="white-shadow">{{ serverInfo.Players }}/{{ serverInfo.MaxPlayers }}</span>
            </div>
        {% else %}
            <span class="offline">OFFLINE</span>
        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav d-lg-flex gap-3">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('index') }}">Strona główna</a>
                </li>
                {% if isAnyArticle %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('article-list') }}">Artykuły</a>
                </li>
                {% endif %}
                {% if areBansSet %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('bans') }}">Lista Banów</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('rule') }}">Regulamin</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('shop') }}">Sklep</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
