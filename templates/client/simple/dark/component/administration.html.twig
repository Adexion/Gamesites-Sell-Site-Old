{% if administration %}
    <div class="adm-gradient"></div>
    <section id="adm">
        <div class="container">
            <div class="row justify-content-center align-items-center">
                <h1 class="text-center">Poznaj administrację naszego serwera!</h1>
                <div class="col-xxl-6 col-8 d-flex justify-content-center align-items-center text-center flex-wrap gap-3">
                    {% set minotar = "https://mc-heads.net/body/" %}
                    {% for a in administration %}
                        <div>
                            <div class="gs-body d-flex align-items-center justify-content-end flex-column">
                                <img src="{{ minotar }}{{ a.username }}" alt="skin {{ a.username }}" width="125">
                            </div>
                            <h2 class="mt-5 mb-4">{{ (a.description ?? ('Next noname' | trans ~ ' ...'))  | raw | nl2br }}</h2>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>
{% endif %}