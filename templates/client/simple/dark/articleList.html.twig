{% extends 'client/simple/dark/base.html.twig' %}

{% block body %}
    <div class="container min-vh-100 mb-5 pb-5">
        <div class="row justify-content-center flex-column">
            <div class="col-lg-12 pb-5 mb-2 text-center">
                <h1 class="mt-5 pt-5">Nowości na naszym serwerze</h1>
                <h2 class="base">Sprawdź co nowego u nas...</h2>
            </div>
            {% for article in articleList %}
                {% include('client/component/article.html.twig') %}
            {% endfor %}
            {% set path = 'article-list' %}
            {% include('client/component/pagination.html.twig') %}
        </div>
    </div>
{% endblock %}